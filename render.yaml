services:
  - type: web
    name: solpay-backend
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && NODE_ENV=development npm ci && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production

