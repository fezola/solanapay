services:
  - type: web
    name: solpay-backend
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm ci && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_EMAIL
        value: fezola004@gmail.com
      - key: CORS_ORIGIN
        value: https://www.solpayservice.xyz,http://localhost:5173

